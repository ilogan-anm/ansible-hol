#!/usr/bin/env ansible-playbook
---
- hosts: csr1
  gather_facts: yes
  tasks:
  - name: "Push stored config to {{ inventory_hostname }}"
    ios_config: src="templates/configs/{{ inventory_hostname }}.j2"
